name: SSH 접속을 통해 EC2 인스턴스에 배포하기

on:
  push:
    branches:
      - doorcs
      - step3

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-24.04

#     steps:
#       # 리포지토리 내 프로젝트 코드 및 JDK 준비
#       - uses: actions/checkout@v3
#       - uses: actions/setup-java@v4
#         with:
#           distribution: 'corretto'
#           java-version: '21'

#       # application-keys.yml 파일 생성
#       - run: echo "${{ secrets.APPLICATION_KEYS }}" > src/main/resources/application-keys.yml

#       # 빌드 수행
#       - run: chmod +x ./gradlew
#       - run: ./gradlew build

#       # SCP로 빌드 결과 전송 후 SSH로 배포 스크립트 실행
#       - uses: mdallasanta/ssh-scp-deploy@v1.2.0
#         with:
#           local: 'build/libs/'
#           remote: '/home/ec2-user/application/'
#           host: ${{ secrets.EC2_HOST }}
#           user: ${{ secrets.EC2_USERNAME }}
#           key: ${{ secrets.EC2_PRIVATE_KEY }}
#           pre_upload: |-
#             mkdir -p /home/ec2-user/application/libs
#             rm -f /home/ec2-user/application/libs/*.jar
#           post_upload: /home/ec2-user/application/deploy.sh
